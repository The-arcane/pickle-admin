
'use client';

// This file can be removed, but we'll create it to ensure the route is registered if it's the first file in the directory.
export default function AdvertisementLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
